import {Contract,JsonRpcProvider} from 'ethers';
const provider=new JsonRpcProvider(process.env.ETHEREUM_RPC_URL || 'https://ethereum.publicnode.com',1);
const contract=new Contract('0xd084B091bAf94154D782f78EB9f904A76d8F741a',['function uri(uint256) view returns (string)','function recipient() view returns (address)'],provider);
try {const [uri,recipient]=await Promise.all([contract.uri(1),contract.recipient()]);console.log(JSON.stringify({contract:await contract.getAddress(),tokenId:1,metadataURI:uri,recipient},null,2));const url=uri.startsWith('ipfs://')?'https://ipfs.io/ipfs/'+uri.slice(7):uri;if(!/^https:\/\//.test(url))throw Error('Metadata URI requires manual inspection');const response=await fetch(url,{signal:AbortSignal.timeout(20000)});if(!response.ok)throw Error('Metadata gateway returned '+response.status);const metadata=await response.json();console.log(JSON.stringify(metadata,null,2));}catch(e){console.error('Live metadata verification failed:',e.message);process.exitCode=1;}
